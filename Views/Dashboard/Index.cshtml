@* @model cine.Controllers.DashboardViewModel

<h2 class="mb-4">لوحة التحكم</h2>

<div class="row text-center mb-5">
    <div class="col-md-4">
        <div class="card shadow border-0">
            <div class="card-body">
                <h5>عدد السيارات</h5>
                <p class="display-6 fw-bold text-primary">@Model.CarCount</p>
            </div>
        </div>
    </div>
    <div class="col-md-4">
        <div class="card shadow border-0">
            <div class="card-body">
                <h5>عدد المستخدمين</h5>
                <p class="display-6 fw-bold text-success">@Model.UserCount</p>
            </div>
        </div>
    </div>
    <div class="col-md-4">
        <div class="card shadow border-0">
            <div class="card-body">
                <h5>عدد الطلبات</h5>
                <p class="display-6 fw-bold text-danger">@Model.OrderCount</p>
            </div>
        </div>
    </div>
</div>

<h4 class="mb-3">أحدث 5 طلبات</h4>
<table class="table table-striped shadow-sm">
    <thead class="table-dark">
        <tr>
            <th>#</th>
            <th>البريد الإلكتروني</th>
            <th>العربية</th>
            <th>التاريخ</th>
            <th>الإجمالي</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var order in Model.LatestOrders)
        {
            foreach (var item in order.OrderItems)
            {
                <tr>
                    <td>#@order.Id</td>
                    <td>@order.User?.Email</td>
                    <td>@item.Car?.Name</td>
                    <td>@order.OrderDate.ToShortDateString()</td>
                    <td>@order.TotalAmount.ToString("C")</td>
                </tr>
            }
        }
    </tbody>
</table>

<hr class="my-5" />

<h4>المستخدمون</h4>
<table class="table table-bordered table-hover shadow-sm">
    <thead class="table-dark">
        <tr>
            <th>البريد الإلكتروني</th>
            <th>الدور</th>
            <th>الإجراء</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var user in Model.UsersWithRoles)
        {
            <tr>
                <td>@user.Email</td>
                <td>
                    @if (user.Role == "Admin")
                    {
                        <span class="badge bg-success">Admin</span>
                    }
                    else
                    {
                        <span class="badge bg-secondary">User</span>
                    }
                </td>
                <td>
                    @if (user.Role != "Admin")
                    {
                        <form asp-action="MakeAdmin" method="post">
                            <input type="hidden" name="userId" value="@user.Id" />
                            <button type="submit" class="btn btn-sm btn-warning">تعيين كـ Admin</button>
                        </form>
                    }
                    else
                    {
                        <span class="text-muted">مشرف</span>
                    }
                </td>
            </tr>
        }
    </tbody>
</table>
 *@